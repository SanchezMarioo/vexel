diff --git a/lib/client.js b/lib/client.js
index 12b72e941823714b678886edb4d39257d6a320eb..dc6e9fcdf139976c39542e8693973d70b0f35464 100644
--- a/lib/client.js
+++ b/lib/client.js
@@ -3,7 +3,6 @@ const stdhttp = require('http');
 const crypto = require('crypto');
 const { strict: assert } = require('assert');
 const querystring = require('querystring');
-const url = require('url');
 const { URL, URLSearchParams } = require('url');
 
 const jose = require('jose');
@@ -64,9 +63,13 @@ function authorizationHeaderValue(token, tokenType = 'Bearer') {
 }
 
 function getSearchParams(input) {
-  const parsed = url.parse(input);
-  if (!parsed.search) return {};
-  return querystring.parse(parsed.search.substring(1));
+  try {
+    const parsed = new URL(input, 'https://op.example.com');
+    if (!parsed.search) return {};
+    return querystring.parse(parsed.search.substring(1));
+  } catch (err) {
+    return {};
+  }
 }
 
 function verifyPresence(payload, jwt, prop) {
@@ -339,7 +342,7 @@ class BaseClient {
       id_token_hint = id_token_hint.id_token;
     }
 
-    const target = url.parse(this.issuer.end_session_endpoint);
+    const target = new URL(this.issuer.end_session_endpoint);
     const query = defaults(
       getSearchParams(this.issuer.end_session_endpoint),
       params,
@@ -356,10 +359,17 @@ class BaseClient {
       }
     });
 
-    target.search = null;
-    target.query = query;
+    target.search = '';
+
+    Object.entries(query).forEach(([key, value]) => {
+      if (Array.isArray(value)) {
+        value.forEach((item) => target.searchParams.append(key, String(item)));
+      } else {
+        target.searchParams.set(key, String(value));
+      }
+    });
 
-    return url.format(target);
+    return target.toString();
   }
 
   callbackParams(input) {
diff --git a/lib/issuer.js b/lib/issuer.js
index 906e6a51a8987654ea86ae6007d301ecfc5d6c9a..6b63ba5e020573797a01004bb6afa000e3121684 100644
--- a/lib/issuer.js
+++ b/lib/issuer.js
@@ -1,5 +1,5 @@
 const { inspect } = require('util');
-const url = require('url');
+const { URL } = require('url');
 
 const { RPError } = require('./errors');
 const getClient = require('./client');
@@ -83,7 +83,7 @@ class Issuer {
 
   static async webfinger(input) {
     const resource = webfingerNormalize(input);
-    const { host } = url.parse(resource);
+    const { host } = new URL(resource);
     const webfingerUrl = `https://${host}/.well-known/webfinger`;
 
     const response = await request.call(this, {
@@ -175,18 +175,18 @@ class Issuer {
 }
 
 function resolveWellKnownUri(uri) {
-  const parsed = url.parse(uri);
+  const parsed = new URL(uri);
   if (parsed.pathname.includes('/.well-known/')) {
-    return uri;
+    return parsed.toString();
+  }
+
+  if (parsed.pathname.endsWith('/')) {
+    parsed.pathname = `${parsed.pathname}.well-known/openid-configuration`;
   } else {
-    let pathname;
-    if (parsed.pathname.endsWith('/')) {
-      pathname = `${parsed.pathname}.well-known/openid-configuration`;
-    } else {
-      pathname = `${parsed.pathname}/.well-known/openid-configuration`;
-    }
-    return url.format({ ...parsed, pathname });
+    parsed.pathname = `${parsed.pathname}/.well-known/openid-configuration`;
   }
+
+  return parsed.toString();
 }
 
 module.exports = Issuer;
diff --git a/lib/passport_strategy.js b/lib/passport_strategy.js
index 523458f8999820103591784b8eb994dc8c879daa..7a695702409b5cb667c3f661163b6f741fec75b6 100644
--- a/lib/passport_strategy.js
+++ b/lib/passport_strategy.js
@@ -1,4 +1,4 @@
-const url = require('url');
+const { URL } = require('url');
 const { format } = require('util');
 
 const cloneDeep = require('./helpers/deep_clone');
@@ -18,6 +18,10 @@ function verified(err, user, info = {}) {
   }
 }
 
+function getIssuerHostname(input) {
+  return new URL(input).hostname;
+}
+
 function OpenIDConnectStrategy(
   { client, params = {}, passReqToCallback = false, sessionKey, usePKCE = true, extras = {} } = {},
   verify,
@@ -39,7 +43,7 @@ function OpenIDConnectStrategy(
   this._verify = verify;
   this._passReqToCallback = passReqToCallback;
   this._usePKCE = usePKCE;
-  this._key = sessionKey || `oidc:${url.parse(this._issuer.issuer).hostname}`;
+  this._key = sessionKey || `oidc:${getIssuerHostname(this._issuer.issuer)}`;
   this._params = cloneDeep(params);
 
   // state and nonce are handled in authenticate()
@@ -72,7 +76,7 @@ function OpenIDConnectStrategy(
     throw new TypeError(`${this._usePKCE} is not valid/implemented PKCE code_challenge_method`);
   }
 
-  this.name = url.parse(client.issuer.issuer).hostname;
+  this.name = getIssuerHostname(client.issuer.issuer);
 }
 
 OpenIDConnectStrategy.prototype.authenticate = function authenticate(req, options) {
